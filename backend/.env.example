# Template only — do NOT put a real key here (this file may be committed).
# Copy to backend/.env and paste your key there instead:
#   cp backend/.env.example backend/.env
#   # then edit backend/.env and set OPENAI_API_KEY

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_TIMEOUT=60
